import React from "react";
import "./Header.css";

const Header = () => {
	return (
		<div className="header">
			<button>Login</button>
			<button>Register</button>
		</div>
	);
};

export default Header;
